; Converted from z88dk: am48_islessequal.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_ISLESSEQUAL

	PUBLIC	am48_islessequal

	EXTRN	am48_dle

	CSEG


; int islessequal(real-floating x, real-floating y)




   ; Return bool (AC <= AC')
   ;
   ; enter : AC  = double x
   ;         AC' = double y
   ;
   ; exit  : HL = 0 and carry reset if false
   ;         HL = 1 and carry set if true
   ;
   ; uses  : af, hl

am48_islessequal	EQU	am48_dle

	END
