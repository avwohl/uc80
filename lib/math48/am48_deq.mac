; Converted from z88dk: am48_deq.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_DEQ

	PUBLIC	am48_deq

	EXTRN	am48_dcmp, error_oc, error_znc

	CSEG





am48_deq:

   ; Return bool (AC == AC')
   ;
   ; enter : AC'= double y
   ;         AC = double x
   ;
   ; exit  : HL = 0 and carry reset if false
   ;         HL = 1 and carry set if true
   ;
   ; uses  : af, hl

	CALL	am48_dcmp

	JP	Z, error_oc	; if x == y true
	JP	error_znc

	END
