; Converted from z88dk: am48_fmin.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_FMIN

	PUBLIC	am48_fmin

	EXTRN	am48_dcmp

	CSEG


; double fmin(double x, double y)




am48_fmin:

   ; return min of double x, double y
   ;
   ; enter : AC  = double x
   ;         AC' = double y
   ;
   ; exit  : AC' = min(x,y)
   ;         AC  = max(x,y)
   ;
   ; uses  : af, bc, de, hl, bc', de', hl'

	CALL	am48_dcmp
	RET	NC	; if AC >= AC'

	EXX	; swap(x,y)
	RET

	END
