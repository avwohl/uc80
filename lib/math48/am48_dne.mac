; Converted from z88dk: am48_dne.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_DNE

	PUBLIC	am48_dne

	EXTRN	am48_dcmp, error_oc, error_znc

	CSEG





am48_dne:

   ; Return bool (AC != AC')
   ;
   ; enter : AC'= double y
   ;         AC = double x
   ;
   ; exit  : HL = 0 and carry reset if false
   ;         HL = 1 and carry set if true
   ;
   ; uses  : af, hl

	CALL	am48_dcmp

	JP	NZ, error_oc	; if x != y true
	JP	error_znc

	END
