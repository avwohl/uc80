; Converted from z88dk: am48_fmax.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_FMAX

	PUBLIC	am48_fmax

	EXTRN	am48_dcmp

	CSEG


; double fmax(double x, double y)




am48_fmax:

   ; return max of double x, double y
   ;
   ; enter : AC  = double x
   ;         AC' = double y
   ;
   ; exit  : AC' = max(x,y)
   ;         AC  = min(x,y)
   ;
   ; uses  : af, bc, de, hl, bc', de', hl'

	CALL	am48_dcmp
	RET	C	; if AC < AC'

	EXX	; swap(x,y)
	RET

	END
