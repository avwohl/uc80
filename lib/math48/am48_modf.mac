; Converted from z88dk: am48_modf.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_MODF

	PUBLIC	am48_modf

	EXTRN	am48_dequate, am48_dip, am48_dsub

	CSEG


; double modf(double value, double *iptr)




am48_modf:

   ; Break AC' into integer and fraction parts
   ;
   ; enter : AC' = double x
   ;
   ; exit  : AC' = frac(x)
   ;         AC  = int(x)
   ;
   ; uses  : af, bc, de, hl, af', bc', de', hl'

	EXX

	CALL	am48_dequate
	CALL	am48_dip

	EXX

	JP	am48_dsub

	END
