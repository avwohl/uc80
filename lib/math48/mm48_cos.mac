; Converted from z88dk: mm48_cos.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME MM48_COS

	PUBLIC	mm48_cos

	EXTRN	mm48__acpi, mm48_fpsub, mm48__sinc

	CSEG





mm48_cos:

   ; cosine
   ; AC' = cos(AC') = sin(pi/2 - AC')
   ;
   ; enter : AC'(BCDEHL') = float x
   ;
   ; exit  : AC' = cos(x)
   ;
   ; uses  : af, af', bc', de', hl'

	PUSH	BC	; save AC
	PUSH	DE
	PUSH	HL

	CALL	mm48__acpi
	DEC	L	; AC = pi/2

	EXX
	CALL	mm48_fpsub	; AC'= pi/2 - x
	JP	mm48__sinc

	END
