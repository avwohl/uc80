; Converted from z88dk: am48_dge.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_DGE

	PUBLIC	am48_dge

	EXTRN	am48_dcmp, error_oc, error_znc

	CSEG





am48_dge:

   ; Return bool (AC >= AC')
   ;
   ; enter : AC'= double y
   ;         AC = double x
   ;
   ; exit  : HL = 0 and carry reset if false
   ;         HL = 1 and carry set if true
   ;
   ; uses  : af, hl

	CALL	am48_dcmp

	JP	NC, error_oc	; if x >= y true
	JP	error_znc

	END
