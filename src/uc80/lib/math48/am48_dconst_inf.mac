; Converted from z88dk: am48_dconst_inf.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_DCONST_INF

	PUBLIC	am48_dconst_inf, am48_dconst_pinf, am48_dconst_minf

	CSEG




am48_dconst_inf:

   ; set AC = +-inf depending on sign

	BIT	7,B
	JR	NZ, am48_dconst_minf

am48_dconst_pinf:

   ; set AC = +inf

	LD	BC,7FFFH

join:

	LD	E,C
	LD	D,C
	LD	H,C
	LD	L,C

	RET

am48_dconst_minf:

   ; set AC = -inf

	LD	BC,0FFFFH
	JR	join

	END
