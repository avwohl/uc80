; Converted from z88dk: am48_ddup.asm
; Auto-generated by z88dk_to_um80.py

	.Z80
	NAME AM48_DDUP

	PUBLIC	am48_ddup

	CSEG




am48_ddup:

   ; duplicate double on top of stack
   ;
   ; enter : stack = double x, ret
   ;
   ; exit  :    AC = double x
   ;         stack = double x, double x
   ;
   ; uses  : af, bc, de, hl

	POP	AF

	POP	HL
	POP	DE
	POP	BC

	PUSH	BC
	PUSH	DE
	PUSH	HL

	PUSH	BC
	PUSH	DE
	PUSH	HL

	PUSH	AF
	RET

	END
